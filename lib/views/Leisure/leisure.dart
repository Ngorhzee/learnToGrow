import 'package:flutter/material.dart';

class Leisure extends StatelessWidget {
  const Leisure({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}